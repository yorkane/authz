# 241.t (aigc) —— authz 测试部署服务器

| 字段 | 值 |
|------|-----|
| 内网 IP | 10.252.25.241 |
| hostname | aigc |
| OS | Ubuntu 22.04.5 LTS |
| CPU/内存 | 8 核 / 16 GiB（无 GPU） |
| 数据盘 | / 293G（LVM），/data 为普通目录 |
| Docker | 28.3.2 + Compose v2.24.5，常规操作需要 sudo |
| NFS | /nas2 已挂载（10.251.14.57:/ptjszx_ai01），可读写 |
| 通配 DNS | *.ws.gatepro.cn -> ws1（218.108.76.10） |

**定位：authz 项目的测试/回归部署机。** 部署验证、回归测试优先在 241.t 上做，避免影响本机（235）与其他生产 box。

## 已部署服务

### authz-test（本项目测试实例）

| 字段 | 值 |
|------|-----|
| 容器名 | `authz-test` |
| 镜像 | `authz:latest`（经 /nas2/docker/authz-test.tar.gz 同步） |
| 工作目录 | `/data/app/authz-test/` |
| compose 文件 | `/data/app/authz-test/docker-compose.yml`（仓库 docker-compose-241t-test.yml 的副本） |
| env 文件 | `/data/app/authz-test/.env`（权限 600） |
| 网络模式 | host |
| HTTP 入口 | `0.0.0.0:6080`（AUTHZ_HTTP_MODE=serve） |
| HTTPS 入口 | `0.0.0.0:6443`（自签证书） |
| Cookie 域 | `.ws.gatepro.cn,.ai-t.wtvdev.com`（AUTHZ_COOKIE_SECURE=false） |
| AUTHZ_HOST_URL | `https://a-241.ai-t.wtvdev.com` |
| 管理员 | `admin` / `test@123` |
| Agent API Key | `AUTHZ_AGENT_API_KEY=ak_3d2027a12abc7170bb7da4f9587dbec9100ac67c92b1707a445eabf7ef17b28e`（seed 为 agent-default，role=admin，loopback_only=1） |
| 数据目录 | `/data/app/authz-test/data` -> `/data`（SQLite v9 schema + 自签证书） |
| 代码挂载 | `admin/`、`lualib/`、`conf/` 从工作区 rsync 挂载（:ro），改代码后重建容器即可 |
| 测试套件 | `/data/app/authz-test/test/`、`scripts/`（rsync 自主机仓库） |
| 依赖镜像 | `redis:8-alpine`（共享会话测试用，经 /nas2 同步） |

2026-09-05：本实例接管 6080/6443，成为域名入口。对外入口
`https://a-241.ai-t.wtvdev.com`（`*-241` / `a-241` 前缀经 APISIX(10.252.25.252) 回源到
241.t 的 6443）。旧实例 `openresty-gateway`（openresty-base 镜像）已停用，容器与数据保留：
数据备份在 `/data/agent/openresty-base/data-backup-20260905`；回滚 = `docker start openresty-gateway`
并改回本实例 .env 端口（6081/6444）后 `up -d --force-recreate`。
注意 `AUTHZ_ADMIN_PASSWORD` 只在首次 seed（users 表为空）时生效，改密码用
`admin_password_reset` 或在用户页修改。

2026-09-06：裸前缀改造上线（commit 11c8bfc）。本实例遗留的 4 条物化域名绑定
（`code-241.ai-t.wtvdev.com` 等）已经 PATCH 迁移为裸前缀（`code`/`codex`/`cc`/`cc2`），
两个泛域入口（ai-t.wtvdev.com / ws.gatepro.cn）代理与菜单均验证可达；回归 566/27/99 全绿。

## 部署/更新流程

```
bash
# 1. 构建（本机，authz 仓库根目录）
docker buildx build --load --build-arg JOBS=8 -t authz:latest .

# 2. 镜像同步：/nas2 中转（禁用 scp）
sudo bash -c 'docker save authz:latest | pigz -p 16 > /nas2/docker/authz-test.tar.gz'
ssh 241.t 'sudo bash -c "gunzip -c /nas2/docker/authz-test.tar.gz | docker load"'

# 3. 代码同步（改 Lua/前端/conf 无需重建镜像，同步+重建容器即可）
rsync -a admin lualib conf scripts test docker-entrypoint.sh 241.t:/data/app/authz-test/

# 4. 重建并验证
ssh 241.t 'cd /data/app/authz-test && sudo docker compose up -d --force-recreate'
ssh 241.t 'curl -sk -o /dev/null -w "%{http_code}\n" https://127.0.0.1:6443/_authz/login'   # 期望 200
```

## 回归测试（在 241.t 上执行）

```
bash
ssh 241.t 'cd /data/app/authz-test && sudo OPENRESTY_TEST_IMAGE=authz:latest bash test/test_authz_gateway.sh'
ssh 241.t 'cd /data/app/authz-test && sudo OPENRESTY_TEST_IMAGE=authz:latest bash test/test_shared_session.sh'
ssh 241.t 'cd /data/app/authz-test && sudo OPENRESTY_TEST_IMAGE=authz:latest bash test/test_klib_router_ctxvar.sh'
```

2026-09-05 全量通过：网关 533、共享会话 27、router/ctxvar 99。
注意 test_shared_session.sh 需要 redis:8-alpine（241.t 无法直连 Docker Hub，缺失时须经 /nas2 同步）。

## 运维

```
bash
ssh 241.t 'sudo docker logs --tail 50 authz-test; sudo docker ps --filter name=authz-test'

# Agent API（仅 241.t 本机回环可用）
ssh 241.t "KEY=\$(sudo grep AUTHZ_AGENT_API_KEY /data/app/authz-test/.env | cut -d= -f2); curl -sk -H \"x-authz-key: \$KEY\" https://127.0.0.1:6443/_authz/api/session"

# 重置 admin 密码
ssh 241.t 'sudo docker exec authz-test admin_password_reset <new-password>'
```

.env 变更后必须 `docker compose up -d --force-recreate`（restart 不会重新读取 env）。
